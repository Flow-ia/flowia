qunad je recharge le solde de comemrcant j'ia eu ces problemes et la recharge n'abboutit pas et donc fix toutes ces erreurs et ces bugs pertinament et efficacement : ou cannot confirm with `off_session=true` when `setup_future_usage` is also set on the PaymentIntent. The customer needs to be on-session to perform the steps which may be required to set up the PaymentMethod for future usage. Please confirm this PaymentIntent with your customer on-session.

POST https://flowia-backend.onrender.com/api/payments/sms/intent 500 (Internal Server Error° 
SUR RENDER / [STRIPE INTENT ERR] You cannot confirm with `off_session=true` when `setup_future_usage` is also set on the PaymentIntent. The customer needs to be on-session to perform the steps which may be required to set up the PaymentMethod for future usage. Please confirm this PaymentIntent with your customer on-session.
[STRIPE INTENT ERR] You cannot confirm with `off_session=true` when `setup_future_usage` is also set on the PaymentIntent. The customer needs to be on-session to perform the steps which may be required to set up the PaymentMethod for future usage. Please confirm this PaymentIntent with your customer on-session.
[STRIPE INTENT ERR] You cannot confirm with `off_session=true` when `setup_future_usage` is also set on the PaymentIntent. The customer needs to be on-session to perform the steps which may be required to set up the PaymentMethod for future usage. Please confirm this PaymentIntent with your customer on-session.

