// Statistiques > Prévisions — IA 7 jours (statsApi.getForecast).
import TabPrevisions from '../settings/TabPrevisions';

export default function Forecast({ theme }) {
  return <TabPrevisions theme={theme}/>;
}
