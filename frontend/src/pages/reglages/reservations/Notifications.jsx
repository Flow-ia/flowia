// Réglages > Réservations > Notifications (récap, rappels, sons, push).
import TabNotifs from '../../settings/TabNotifs';

export default function Notifications({ theme, showToast }) {
  return <TabNotifs theme={theme} showToast={showToast}/>;
}
