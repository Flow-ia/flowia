// Réglages > Mon commerce > Photos (logo, cover, profil).
import TabImages from '../../settings/TabImages';

export default function Photos({ theme, showToast }) {
  return <TabImages theme={theme} showToast={showToast}/>;
}
