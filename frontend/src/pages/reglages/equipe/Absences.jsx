// Réglages > Équipe > Absences (8 types + Confirm pastel).
import TabAbsences from '../../settings/equipe/tabs/TabAbsences';

export default function Absences({ employees, theme }) {
  return <TabAbsences employees={employees} theme={theme}/>;
}
